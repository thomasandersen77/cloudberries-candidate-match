# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-java-with-maven

name: Java CI with Maven

on:
  push:
    branches: [ "**" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      - name: Build with Maven
        run: mvn -B package --file pom.xml


      - name: Call Gemini API and Parse Response
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          JSON_PAYLOAD: ${{ secrets.JSON_PAYLOAD }}
        run: |
          # Debug: Print payload before sending
          echo "JSON payload:"
          echo "$JSON_PAYLOAD"

          curl -s -X POST "https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash-latest:generateContent?key=$GEMINI_API_KEY" \
            -H "Content-Type: application/json" \
            -d "$JSON_PAYLOAD" > gemini_response.json

          # Debug: Print response after request
          echo "Gemini API response:"
          cat gemini_response.json

          if ! jq . gemini_response.json > /dev/null 2>&1; then
            echo "::error::Ugyldig JSON fra Gemini API"
            cat gemini_response.json
            exit 1
          fi

          REVIEW_COMMENT=$(jq -r '.candidates[0].content.parts[0].text // ""' gemini_response.json)
          if [[ -z "$REVIEW_COMMENT" ]]; then
            echo "::error::Klarte ikke å hente ut en gyldig kommentar fra Gemini-responsen."
            cat gemini_response.json
            exit 1
          fi

      # Add here any Perl commands needed, but DO NOT use unsupported regex modifiers like /r or /t
      # Example:
      # - name: Safe Perl Replacement Example
      #   run: perl -pe 's/old/new/g' input.txt > output.txt